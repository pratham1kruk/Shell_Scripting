note 

RUN make up for the first time during setup for later start use docker compose up
download docker-compose and make before running setup:
sudo apt-get update
sudo apt-get make
sudo apt-get install docker-compose -y

brevo email is used for email and number verification and required when you forget or reset password\
setup brevo key and email beforehand in secret.txt
 
use command this command to get read write and execution permission for running the script without error

chmod 700 <filename> run this for all shell script 

create backups named folder in this same dir

use crontab -e command to open cron vim file and remove hastage(#) to reactivate cron job and start routine backup
 
